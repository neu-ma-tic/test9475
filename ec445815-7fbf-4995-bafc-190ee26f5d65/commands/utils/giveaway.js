const { MessageEmbed } = require('discord.js');

module.exports = {
    name: 'giveaway',
    category: 'giveaway',
    description: 'giveaway',
    run: async (bot, message, args, config) => {



    }
}